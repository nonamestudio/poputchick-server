extends layout

block content
    h3.
        Users
    ul
        each user, i in userlist
            li#user_list_item
                p Username : #{user.name}, email:#{user.email}, phone:#{user.phone}